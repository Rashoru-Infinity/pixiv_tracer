version: '3'

services:
  pixiv_tracer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pixiv_tracer
    restart: always
    environment:
      COOKIE: "xxx"
      INTERVAL: 10
      DISCORD_ENDPOINT: "xxx"
      SLACK_ENDPOINT: "xxx"
      USER: "xxx"
      ID: xxx
